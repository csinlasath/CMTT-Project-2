// import { auth } from "firebase-admin";

// var authenticate = require("./middleware/auth");

// export default (userRoutes) => {
//     userRoutes.get("/addpet", authenticate.is (req, res) => {
//         return handle(req, res);
//     });
// }